<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>FreePD.com - Site Closed</title>
    
    <style>
        /* CSS Variables for easy color adjustments */
        :root {
            --color-bg-dark: #1a1a1a;
            --color-card-bg: #1f2937; /* Darker gray-800 equivalent */
            --color-text-main: #ffffff;
            --color-text-secondary: #d1d5db; /* gray-300 */
            --color-accent: #4b8a9d; /* cyan-400 equivalent */
            --color-border: #374151; /* gray-700 */
        }

        /* Base styles and centering */
        body {
            font-family: 'Inter', sans-serif;
            background: var(--color-bg-dark);
            /* Custom gradient background */
            background-image: linear-gradient(135deg, #1f2937 0%, #0f172a 100%);
            
            /* Centering using Flexbox */
            display: flex;
            align-items: center;
            justify-content: center;
            min-height: 100vh;
            padding: 1rem; /* p-4 */
            margin: 0;
            box-sizing: border-box;
        }

        /* Main Card Container */
        .shutdown-card {
            max-width: 40rem; /* max-w-xl is 36rem, using a slightly larger size for readability */
            width: 100%;
            background-color: var(--color-card-bg);
            color: var(--color-text-main);
            border-radius: 0.75rem; /* rounded-xl */
            border: 1px solid var(--color-border);
            
            /* Shadow and Transition for visual effect */
            box-shadow: 0 20px 25px -5px rgba(0, 0, 0, 0.2), 0 10px 10px -5px rgba(0, 0, 0, 0.04); /* shadow-2xl */
            transition: box-shadow 300ms ease-in-out;
        }

        .shutdown-card:hover {
            box-shadow: 0 25px 50px -12px rgba(0, 0, 0, 0.4), 0 0 15px rgba(75, 138, 157, 0.5); /* hover:shadow-3xl */
        }

        /* Default padding for the card (p-8) */
        .card-content {
            padding: 2rem;
        }

        /* Media query for larger screens (md:p-12) */
        @media (min-width: 768px) {
            .card-content {
                padding: 3rem; 
            }
        }

        /* Icon styling */
        .icon-container {
            display: flex;
            justify-content: center;
            margin-bottom: 1.5rem; /* mb-6 */
        }
        
        .icon-svg {
            width: 4rem; /* w-16 */
            height: 4rem; /* h-16 */
            color: var(--color-accent); /* text-cyan-400 */
        }

        /* Header styling */
        .header-content {
            text-align: center;
            margin-bottom: 1.5rem; /* mb-6 */
        }
        
        .main-title {
            font-size: 2.25rem; /* text-4xl */
            font-weight: 800; /* font-extrabold */
            color: var(--color-accent);
            margin-bottom: 0.5rem; /* mb-2 */
            letter-spacing: -0.025em; /* tracking-tight */
            line-height: 1.1;
        }
        
        .sub-title {
            font-size: 1.25rem; /* text-xl */
            color: #9ca3af; /* gray-400 */
        }

        /* Section text styling */
        .section-content {
            text-align: center;
            margin-bottom: 2rem; /* mb-8 */
            padding: 0 1rem;
        }

        .section-content p {
            font-size: 1.125rem; /* text-lg */
            color: var(--color-text-secondary); /* text-gray-300 */
            line-height: 1.625; /* leading-relaxed */
            margin-top: 1rem; /* space-y-4 */
        }
        .section-content p:first-child {
            margin-top: 0;
        }

        /* Footer styling */
        .footer-content {
            text-align: center;
            padding-top: 1rem; /* pt-4 */
            border-top: 1px solid var(--color-border); /* border-t border-gray-700 */
        }
        
        .footer-content p {
            font-size: 0.875rem; /* text-sm */
            color: #6b7280; /* gray-500 */
        }
    </style>
</head>
<body>

    <!-- Main Content Container -->
    <div class="shutdown-card">
        <div class="card-content">
            

            <!-- Shutdown Message -->
            <header class="header-content">
                <h1 class="main-title">
                    The Music Has Moved On.
                </h1>
                <p class="sub-title">
                    A Note on FreePD.com's Closure
                </p>
            </header>

            <section class="section-content">
                <p>
                    Thank you for being part of the FreePD community. After 17 years of sharing millions of free-to-use, Public Domain music downloads with creators worldwide, we have officially taken the service offline.
                </p>
                <p>
                    The hosting and maintenance of the site have ceased. We sincerely appreciate the support and creativity you all brought to the public domain music movement.
                </p>
            </section>

            <!-- Final Note -->
            <footer class="footer-content">
                <p>
                    [freepd.com] is now permanently closed. <BR>
		We wish you the best in your creative endeavors.<BR>2008-2025
                </p>
            </footer>
        </div>
    </div>

</body>
</html>

